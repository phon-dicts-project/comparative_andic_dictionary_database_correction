- [x] простые замены несуществующими символами  
  - [x] `/ / I / l = ӏ`  
  - [x] `||| / || = ;`  
  - [x] `{}` = `()`

- [x] учесть исправления ошибок в `meaning_ru_comment`

- [x] заполнить пустые `definition`

- [x] проверить случаи, когда `definition` начинается с (пробела+) знака препинания, кроме `:` / `...` / `?` (часто ошибка деления)

- [x] проверить случаи, когда в `definition` есть латинские буквы  
  - [x] `O = х̄` (багвалинский, годоберинский)  
  - [x] `K = ч̄ӏ` (багвалинский, тиндинский)  
  - [x] `aw? / awe / ewe = гиг.` (чамалинский)  
  - [x] остальное вручную

- [ ] унифицировать (?)
  - [ ] `meaning_ru` для случаев, когда это ссылка (ссылка / ссылка на форму / значение / значение формы)  
  + иногда ссылки вообще не занесены в `definition`, если значений несколько
  - [ ] `meaning_ru` для случаев, когда в `definition приводится выражение (значение выражения / значение слова / “часть выражения”)
    - [x] иногда морфология есть в `definition` и `morphology`, а иногда только в `morphology`, а иногда только в `definition`

- [ ] другие ошибки распознавания:
  - [ ] варианты через слеш иногда не разделяются и записываются в одно слово
  - [ ] иногда сохраняются переносы слов
  - [ ] не считывается пробел
  - [ ] съезжает диакритика
  - [ ] список замен (совсем неполный, это то, что кому-то попалось):
    -  ошибка → исходн.
    - `ӏ` → `!`
    - `й` → `и́`
    - `ё` → `с̄ / а̄`
    - `Н / н / Ø` → `ᴴ`
    - `о́` → `кӏ` (багвалинский)
    - `n` → `ъ` (ботлихский)
    - `6` → `б` (каратинский)
    - `а` → `с` (тиндинский, багвалинский, в т.ч. в леммах)
    - `е` → `лл` (тиндинский, багвалинский, в т.ч. в леммах)
    - `ь` → `ъ` (каратинский, в т.ч. в леммах)
    - `п` → `н` (андийский, в т.ч. в леммах)

некоторые проблемы для отдельных языков:

ахвахский  
- [ ] нет первой статьи  
- [ ] иногда сохраняется знак переноса

андийский  
- [ ] нет части морфологических / диалектных помет  
- [ ] иногда `п` распознаётся как `н`, в т.ч. в леммах (?)

багвалинский  
- [ ] не распознаются `II`, `III`
- [ ] диакритика съезжает, ударение иногда распознается как `̄`
- [ ] `Ý` вместо `м̅`
- [ ] `о́` вместо `кӏ` (?)
- [ ] `«` вместо `э́` (в `definition`)
- [ ] `â` вместо `з̄`
- [ ] `а` вместо `с` (?)
- [ ] `е` вместо `лл` (?)
- [ ] варианты через слеш иногда записываются в одну лемму

ботлихский (Saidova, Abusov 2012)  
- [ ] нет части морфологических / диалектных помет  
- [ ] иногда сохраняется знак переноса  
- [ ] `n` вместо `ь` (?)
- [ ] когда статья для слова из словосочетания (вида `x: y x`),  
в словосочетании нет пробела и остаётся капс

чамалинский  
- [ ] ударение не всегда распознаётся в `definition`
- [ ] `й` вместо `ӣ / и́`
- [ ] съезжает диакритика

тукитинский / каратинский  
- [ ] иногда сохраняются переносы
- [ ] диакритика не распознаётся / съезжает
- [ ] `ё` вместо `е̄`
- [ ] `6` вместо `б` (?)
- [ ] `ь` вместо `ъ`, в т.ч. в леммах (?)

тиндинский  
- [ ] `х̅` не распознаётся, `Х̅` как `х̅`
- [ ] проблемы с разделением:
  - [ ] иногда лемма повторяется в `definition`
  - [ ] указывается только часть словосочетания
  - [ ] морфология не полностью отделяется
- [ ] `е` вместо `лл`, в т.ч. в леммах (?)
- [ ] `а` вместо `с`, в т.ч. в леммах (?)
- [ ] варианты через слеш иногда записываются в одну лемму
